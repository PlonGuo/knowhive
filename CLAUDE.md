# knowhive — 项目规则

## 代码名词清单（GLOSSARY.md）

**问题**：你回答里随口一个 `rrfFuse`、`expandToParents`、`DocumentIR`，我根本不知道那是干嘛的。
你就算把代码贴出来我也没法当场看懂——**我缺的不是代码，是"这东西存在的理由"**。

**规矩**：本项目在 `learnings/GLOSSARY.md` 维护一份名词清单，
记录所有函数名、对象名、类型名、常量名，三种状态：

| 标记 | 含义 | 你要做什么 |
|---|---|---|
| `[ ]` | 我还不懂 | **本次回答里必须停下来解释**：一句大白话 + 一个比喻 + 为什么需要它 |
| `[~]` | 你讲过了，等我确认 | 我说"懂了"你才能打勾；我没说就还是要解释 |
| `[x]` | 我确认懂了 | 以后直接写名字，**不要再解释**，别啰嗦 |

**执行细节**：
- 回答涉及某个符号前，先看清单里它是什么状态。一次回答里冒出多个 `[ ]`，
  **全都要解释**，不许挑一个讲了就往下跑
- 讲完当场把清单里那行从 `[ ]` 改成 `[~]`，并补上你写的那句大白话（一行，别写长）
- 我说"懂了 / 这个明白了 / 打勾"之后，改成 `[x]`
- 清单里没有的新符号（新写的代码、内部函数、第三方库的 API），**照样要解释，然后补一行进清单**
- 清单只存"一句话是什么"，完整比喻留在对话里/教学文档里，别把清单写成文档

**判断标准**：如果我要靠猜才知道你在说哪个东西，就是你没做到。
